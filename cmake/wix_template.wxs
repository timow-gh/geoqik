<?xml version="1.0" encoding="UTF-8"?>

<?include "cpack_variables.wxi"?>

<!--
  Custom CPack WiX (v3) template. Based on CMake's built-in
  Modules/Internal/CPack/WIX-v3/WIX.template.in, extended to use the
  WixUI_InstallDir dialog set so the installer always shows a directory picker
  with a Browse button.

  WixUI_InstallDir drives the install location through the directory whose Id is
  named by the WIXUI_INSTALLDIR variable. CPack roots its generated directory
  tree at Id="INSTALL_ROOT", so WIXUI_INSTALLDIR points directly at INSTALL_ROOT
  and no aliasing custom action is needed.

  WixUI_InstallDir has no per-user/all-users choice, so the install is forced
  per-machine (ALLUSERS=1) into Program Files by default; the user can Browse to
  any other directory.

  When updating CMake, diff this against the current built-in template to pick up
  any upstream changes.
-->
<Wix xmlns="http://schemas.microsoft.com/wix/2006/wi"@CPACK_WIX_CUSTOM_XMLNS_EXPANDED@
    RequiredVersion="3.6.3303.0">

    <Product Id="$(var.CPACK_WIX_PRODUCT_GUID)"
        Name="$(var.CPACK_PACKAGE_NAME)"
        Language="1033"
        Version="$(var.CPACK_PACKAGE_VERSION)"
        Manufacturer="$(var.CPACK_PACKAGE_VENDOR)"
        UpgradeCode="$(var.CPACK_WIX_UPGRADE_GUID)">

        <!-- WixUI_InstallDir offers no scope choice, so install per-machine. -->
        <Package InstallerVersion="301" Compressed="yes" InstallScope="perMachine"/>

        <Media Id="1" Cabinet="media1.cab" EmbedCab="yes"/>

        <MajorUpgrade
            Schedule="afterInstallInitialize"
            AllowSameVersionUpgrades="yes"
            DowngradeErrorMessage="A later version of [ProductName] is already installed. Setup will now exit."/>

        <WixVariable Id="WixUILicenseRtf" Value="$(var.CPACK_WIX_LICENSE_RTF)"/>

        <!-- WixUI_InstallDir integration. The dialog set edits the directory
             whose Id is named here; point it at CPack's INSTALL_ROOT so the
             browsed path anchors CPack's whole generated file tree. Force a
             per-machine install so the default location is Program Files. -->
        <Property Id="WIXUI_INSTALLDIR" Value="INSTALL_ROOT"/>
        <Property Id="ALLUSERS" Value="1"/>

        <?ifdef CPACK_WIX_PRODUCT_ICON?>
        <Property Id="ARPPRODUCTICON">ProductIcon.ico</Property>
        <Icon Id="ProductIcon.ico" SourceFile="$(var.CPACK_WIX_PRODUCT_ICON)"/>
        <?endif?>

        <?ifdef CPACK_WIX_UI_BANNER?>
        <WixVariable Id="WixUIBannerBmp" Value="$(var.CPACK_WIX_UI_BANNER)"/>
        <?endif?>

        <?ifdef CPACK_WIX_UI_DIALOG?>
        <WixVariable Id="WixUIDialogBmp" Value="$(var.CPACK_WIX_UI_DIALOG)"/>
        <?endif?>

        <FeatureRef Id="ProductFeature"/>

        <UIRef Id="WixUI_InstallDir" />
        <UIRef Id="WixUI_ErrorProgressText" />

        <?include "properties.wxi"?>
        <?include "product_fragment.wxi"?>
    </Product>
</Wix>
